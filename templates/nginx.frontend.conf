# Cấu hình nginx phục vụ SPA: mọi đường dẫn không phải file
# đều trả index.html để React Router xử lý phía client.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
